/*Write a function, `printAllKeyValuePairs(obj)` which takes an object 
with unknown content and prints to console, one per line, each **key** and **value** in the object.

e.g.:

Given the following object as input,

const characters = {
  Bruce: "Hulk",
  Peter: "Spiderman",
  Ororo: "Storm"
};
*/

/*
    function printAllKeyValuePairs(obj)

   console.log( obj. entries )
*/